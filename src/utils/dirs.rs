use crate::utils::common;
use std::path::Path;
use std::path::PathBuf;

/// This will get the git repositories from the directory if specified.
pub fn get_repos(dir: Option<&Path>) -> Vec<PathBuf> {
    todo!();
}
